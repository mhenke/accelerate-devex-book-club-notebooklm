{
  "name": "accelerate-devex-book-club-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Jekyll site refactoring confidence",
  "scripts": {
    "test": "dotenv -e ../.env -- playwright test",
    "test:headed": "dotenv -e ../.env -- playwright test --headed",
    "test:ui": "dotenv -e ../.env -- playwright test --ui",
    "test:debug": "dotenv -e ../.env -- playwright test --debug",
    "test:ci": "playwright test",
    "test:percy": "dotenv -e ../.env -- playwright test",
    "test:report": "playwright show-report",
    "test:codegen": "dotenv -e ../.env -- playwright codegen",
    "install-browsers": "playwright install"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.8.2",
    "@playwright/test": "^1.40.0",
    "dotenv": "^17.2.2",
    "dotenv-cli": "^10.0.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0"
  },
  "keywords": [
    "playwright",
    "testing",
    "jekyll",
    "refactoring",
    "e2e"
  ],
  "author": "DevEx Book Club",
  "license": "MIT"
}
